# 游戏按钮映射 V0.5 Profile 管理计划

## 目标

在不改变低风险单屏蔽区域和高风险单按钮映射边界的前提下，为两种模式提供统一的多方案配置管理、切换、复制和删除保护。

## 阶段

1. 梳理现有 `LayoutPrefs`、`MappingPrefs`、编辑器和 Overlay 服务读取链路。
2. 建立统一 `ProfileManager`，使用现有 SharedPreferences 体系保存 Profile，并迁移旧单套数据到默认方案。
3. 让低风险和高风险配置读写均通过当前 Profile。
4. 增加共用方案管理 UI：当前方案、列表、创建、复制、删除保护和切换。
5. 构建检查；运行时设备验证只覆盖配置切换与低风险/测试界面，不启动真实游戏。

## 阶段状态

- [x] 梳理现有配置、编辑器和 Overlay 读取链路。
- [x] 建立统一 `ProfileManager`，并保留旧配置用于迁移。
- [x] 低风险与高风险读写接入当前 Profile。
- [x] 完成当前方案、列表、新建、复制、切换和删除保护 UI。
- [x] `lintDebug`、`assembleDebug` 和两个模块的 `testDebugUnitTest` 任务检查通过。
- [ ] 真机运行时验证：按用户要求留给用户自行执行，当前未验证。

## 明确边界

- 每个低风险 Profile 仍只有一个屏蔽区域和一个悬浮开关。
- 每个高风险 Profile 仍只有一个目标位置和一个虚拟按钮。
- 不增加自动识别、场景自动切换、OCR、AI、云同步或多按钮。
- 删除当前 Profile 必须先切换到其他同模式 Profile。
