# 游戏按钮映射 V0.5 用户验收清单

## 低风险

1. 进入低风险方案，确认存在“默认方案”。
2. 新建空白方案，保存屏蔽区域，返回后确认方案名称和位置保持。
3. 复制当前方案，修改屏蔽区域或悬浮开关位置，确认原方案不变。
4. 切换回原方案，确认其布局恢复；切换到复制方案，确认修改后的布局恢复。
5. 启动屏蔽后切换方案，确认运行中的 Overlay 使用新方案。
6. 选择当前使用方案并点击删除，确认被拒绝；切换到其他方案后再删除非当前方案。

## 高风险

1. 仅使用自建 `input-test-app`，不要启动或测试任何真实游戏。
2. 分别为两个方案设置不同目标位置和虚拟按钮位置。
3. 切换方案，确认编辑器和运行中的映射读取对应方案。
4. 确认高风险方案的删除保护与低风险一致。
