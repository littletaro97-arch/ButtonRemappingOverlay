# 游戏按钮映射 · 版本更新记录（UPDATE.md）

> **维护约定：每次版本变更（功能、修复、回退）后，都必须在本文件追加/修订对应版本的条目。**
> 文档位置：`E:\按钮重映射\UPDATE.md`（项目工作区根目录）。
> 版本号规范：`x.y.z`（破坏性/大版本升 y，修复升 z）；versionCode 递增。
> 交付目录：`E:\Deepseek output\游戏按钮映射\`（APK + 各版本 BUILD_VERIFICATION/PHASE_ACCEPTANCE/MANUAL_DEVICE_CHECKLIST 文档）。

---

## 当前版本

### v6.3（2026-08-18，versionCode 36）— 高风险重进屏蔽失效修复 + 目标/屏蔽区域透明度

**背景**：实测发现高风险（修改键位）方案在"启动方案 → 进入游戏 → 切换后台 → 再次进入游戏"（含指定应用启动、以及进入游戏触发横竖屏/尺寸变化导致映射重建）时，有概率出现目标位置拦截窗口缺失：目标按钮恢复可直点，而虚拟按钮仍能注入。另需为目标位置区域与屏蔽区域提供透明度调节。

**处理**：

1. **目标拦截窗口单一真源（核心修复）**：`HighRiskOverlayService.addVirtualView` 改为直接用 `config.targetBlock` 重新换算目标矩形再挂窗，不再依赖可能被 `removeVirtualView()` 清空的 `targetBlockRect` 字段；`removeVirtualView` 对目标窗口无条件尝试移除，避免残留旧拦截窗。修复旋转重建/指定应用重启映射后"目标按钮无法屏蔽、新按钮仍可触发"的偶发问题。
2. **新增目标位置透明度**：`MappingConfig` 增加 `targetBlockAlpha`（默认 38%，范围 5%–100%），运行时目标拦截窗口显示为半透明橙色标识；高风险编辑器底部栏增加"目标位置透明度"滑块（复用长按编辑器透明度逻辑），屏蔽逻辑不变。
3. **屏蔽区域透明度**：`OverlayService` 运行时屏蔽区改由 `blockedAreaAlpha` 控制显示为半透明红色标识（此前运行时恒为全透明、仅编辑器可见）；触摸拦截逻辑不变。
4. **高风险降级为中风险**：首页"修改键位模式"标记改为与长按触发一致的黄色"中风险方案"，入口保留二次弹窗确认（取消/继续）。
5. **模式页标题**：屏蔽区域模式与修改键位模式顶部标题分别改为"屏蔽区域模式"/"修改键位模式"。
6. **首页文案精简**：移除"请选择模式"、"修改键位模式仅用于受控测试"、"修改键位模式禁止真实游戏测试、连点、宏、自动操作、Root、Accessibility 和反作弊规避"三处文本。
7. **问题反馈三栏**：改为左中右三栏——左侧小红书/bilibili 圆形图标、中间联系方式、右侧"点击跳转"，点击整行均可跳转。
8. **指定应用启动修复**：修复"添加应用"多选后应用条目横向堆叠的问题（触发应用容器改为纵向排列）。

**已执行验证**：

- `lintDebug` + `assembleDebug`：通过，0 errors，49 个 warning（均为既有风格/弃用类警告，新增 1 个来自新目标透明度标签的 SetTextI18n）。
- 纯策略单元测试沿用 v6.2（本轮未改动策略；中文路径下测试工作进程的类路径限制同上）。

---

## 历史版本

### v6.2（2026-08-18，versionCode 35）— 目标几何统一 + 长按滑动误触修复

**背景**：复核 v6.1 后确认其结论存在两处矛盾：编辑器显示用户划定的 `targetBlock`，运行时却把拦截窗隐式放大到至少屏幕 14%×18%；模拟 Tap 又取放大/边缘夹取后矩形的中心。因此显示区域、真实屏蔽区域和注入点不是同一几何语义。长按触发视图也完全没有处理 `ACTION_MOVE`，触点滑离后仍会继续计时并误触发。

**处理**：

1. **目标区域单一真源**：编辑器显示、保存配置、运行时屏蔽窗口全部直接使用同一个 `OverlayGeometry.toPixelRect(config.targetBlock, ...)` 结果；删除 v6.1 的 14%×18% 隐式放大。需要覆盖更大的游戏按钮热区时，必须在编辑器中把可见目标矩形调大，避免隐藏屏蔽范围。
2. **Tap 固定命中用户目标几何中心**：注入点只取原始目标矩形中心，不再从放大/夹取后的拦截矩形派生；屏幕边缘目标也不会因扩张夹取而偏移。
3. **完整矩形语义**：目标编辑框固定显示为矩形，不再提供目标圆角调节，明确 WindowManager 实际输入命中区包含四角；旧配置字段保留以兼容历史 Profile，但运行时不以圆角改变命中边界。
4. **目标输入层稳健性**：不再把目标拦截 View 设为 `alpha=0`，改为 View alpha 1 + `1/255` 的不可见绘制表面，规避部分 OEM 对完全透明 Overlay 的合成/输入优化不确定性。
5. **长按资格状态机**：按下后，只要移动距离超过系统 `touchSlop`、触点离开区域、出现第二指或收到系统取消，本次长按资格立即永久取消；继续停留超过阈值也不会触发，必须抬起后重新按下。应用内操作日志会记录取消原因。

**已执行验证**：

- 纯策略单元测试 4/4 通过：目标中心（含屏幕边缘矩形）、静止容差、超距移动、离区/多指取消。
- `lintDebug`：0 errors（48 个既有 warning）；`assembleDebug`：通过。
- APK：`E:\Deepseek output\游戏按钮映射\button-remapping-v6.2-debug.apk`。
- APK SHA-256：`5192275A5FF83FD0F8BACD6B4CCE3FF94A5A852437CD47A7AD3EBB322C110DE1`。

**未执行边界**：遵照要求未使用 ADB；真实游戏内“目标直点被吞、虚拟按钮命中目标中心、边缘目标、滑出后长按不触发”均为 `NOT RUN`，需要用户按 V6.2 清单手工确认。注入时目标 Overlay 必须短暂让路，仍存在极短的人手并发竞争窗口，不能宣称绝对消除。

### v6.1（2026-08-18，versionCode 34）— 高风险改键位拦截失效修复

**背景**：v5.9 实测发现高风险"改键位"方案失效——划定的目标位置按钮仍然可以直接点击。
**处理**：v6.0 代码全部删除（详见下方 v6.0 条目，判定完全写坏），回退到 v5.9 代码后实施以下修复：

1. **目标拦截窗口尺寸下限（核心修复）**：v5.9 把运行时目标拦截窗口从"固定 14%×18% 的放大拦截区"改为"用户划定的精确矩形"，若用户划定的目标框略小于游戏按钮（或旧配置尺寸异常），按钮边缘/触摸热区露出，仍可点击。修复：拦截窗口以用户划定位置为中心，**尺寸不小于屏幕的 14%×18%**（恢复 v5.8 的保证），真实覆盖目标按钮。
2. **编辑器保存防写坏**：`HighRiskEditorActivity.syncConfigFromViews` 增加守卫——视图尚未按配置定位（首次布局未完成）时禁止回写，避免把初始布局矩形当成目标位置保存导致配置损坏。
3. **拦截窗口恢复健壮性**：注入完成后恢复拦截窗口触摸时不再因 `!active` 跳过（防止 `FLAG_NOT_TOUCHABLE` 残留导致目标位置永久失去拦截）；隐藏失败时标记窗口为未挂载，恢复时重新添加（自愈系统静默移除的窗口）。

**真机验证**（OPLUS PKT110，1216x2640）：
- 拦截窗口尺寸下限生效（133×133 → 170×475）；
- 拦截窗口内点击被吞掉（下层元素无响应）；拦截窗口外同一按钮正常响应（对照组）——"按钮仍可点击"修复；
- 构建 lint + assemble：0 errors。

**注意**：v6.1 基于 v5.9 代码（v6.0 的方案 B 滑动放行全部移除），因此"滑动经过屏蔽区会被打断"的问题在 v6.1 中仍存在（见 v6.0 条目），如需恢复需另行评估。

### v1.6.0（2026-08-18，versionCode 33）— ⚠️ 已删除回退（勿再用）

**"方案 B"大版本：屏蔽区只吞点按、放行滑动。**
- 屏蔽窗智能分类（touchSlop 判定点按/滑动），滑动经无障碍 `dispatchGesture` 补发下层；
- 所有屏蔽窗去掉 `FLAG_SPLIT_TOUCH`（起点在区外的滑动原生直通）；
- 注入期间临时移除/恢复窗口，真机三模式均 5/5 单次补发验证通过。

**判定：完全写坏，代码已全部删除回退至 v5.9。** 原因与遗留问题：
- 滑动补发为合成手势，拖拽跟手类操作手感劣化；
- 无障碍服务在该 OPLUS 设备上会周期性重启，补发不稳定；
- 交互复杂度显著上升，与项目"稳定优先"的原则冲突。
- 该版本不再交付使用；如未来要恢复"滑动放行"，需重新设计（建议先做"仅去 FLAG_SPLIT_TOUCH"的轻量方案并长时间真机验证）。

### v1.5.9（2026-08-17，versionCode 32）— 高风险目标位置所见即所得
- 高风险目标从"仅中心点"改为完整"位置+尺寸"组件（`targetBlock`），编辑器显示/保存、运行时拦截窗共用同一换算，往返一致；旧配置（仅中心点）自动迁移。
- 目标拦截窗与注入点改用同一矩形，与编辑器完全一致。

### v1.5.8（2026-08-17，versionCode 31）— 高风险位置/尺寸不一致修复
- 编辑器方向跟随截图方向（竖屏截图不再被横屏拉伸），显示/保存统一用 `coordinateRotation` 往返换算；
- 停止映射后页面按钮不及时恢复蓝色：各服务停止入口同步翻转 `isRunning`。

### v1.5.7（2026-08-17，versionCode 30）— 主页过渡动画统一
- 低风险页面拆为独立 `LowRiskActivity`，经 `startActivity` 进入，与中/高风险统一获得系统水平推入过渡（替换 v1.5.6 的淡入方案）。

### v1.5.6（2026-08-17，versionCode 29）
- 屏蔽区域过渡动画修复：同 Activity 视图切换改用根视图 240ms 淡入（v1.5.5 的 overridePendingTransition 对视图切换无效）。

### v1.5.5（2026-08-17，versionCode 28）
- 合并 codex fix 分支（v1.5.2–v1.5.4）修复：无障碍注入主线程/超时兜底、注入前 FLAG_NOT_TOUCHABLE + waitForInputWindowUpdate、坐标统一、虚拟按钮绘制内缩；
- 过渡动画对齐（初版）、编辑器遮挡栏隐藏/重现、三方案互斥、高风险警告文案、隐藏 Shizuku 设置模块。

### v1.5.4 / v1.5.3 / v1.5.2（2026-08-17，codex fix 分支）
- 分支修复：注入前临时移除目标/长按屏蔽窗口、完成后恢复；无障碍 dispatchGesture 返回值判定、统一主线程提交、异步完成/取消/超时回调（防屏蔽层永久隐藏）；长按标签由"高风险"改为黄色"中风险方案"。
- v1.5.2 有独立文档（BUILD_VERIFICATION-v1.5.2-codex-fix.md / PHASE_ACCEPTANCE-v1.5.2-codex-fix.md）；**v1.5.3 / v1.5.4 仅存在对应 APK，无验证文档**，其内容由 v1.5.5 的"同步 Codex 修复（V1.5.4 → 工作区基线）"合并记录覆盖。

### v1.5.1（2026-08-17）
- 长按进度显示改为区域外部轮廓；修复进度满不触发（Shizuku 预绑定 + 无障碍回退）；长按标签改"高风险方案"+风险确认；删除滚动条。

### v1.5.0（2026-08-17）— 新增长按触发模式（第三模式）
- 编辑长按区域、按下计时进度环、长按满注入一次 tap、提前松开取消；复用注入后端、指定应用启动、方案管理、时长调节 300–3000ms。

### v1.4.5（2026-08-17）
- 修复注入 tap 被自家拦截窗吞掉：注入前临时隐藏目标拦截窗，完成后恢复。

### v1.4.4（2026-08-17）
- 修复目标拦截窗盖住虚拟按钮：先加拦截窗（下层）再加虚拟按钮（上层）。

### v1.4.3（2026-08-17）
- 新增目标位置触摸屏蔽：映射运行时在目标位置创建透明拦截窗口，玩家直点目标无效，只能通过虚拟按钮注入。

### v1.4.2（2026-08-17）
- 修复横竖屏切换后映射失效自动退出（按当前几何重建映射）；
- 修复竖屏设置后运行时位置旋转 180°（编辑器保存/恢复统一 coordinateRotation）。

### v1.4.1（2026-08-17）
- 修复无障碍已连接仍强制要求 Shizuku：启动检查改为"Shizuku 或无障碍任一就绪"即放行。

### v1.4.0（2026-08-17）
- 新增无障碍注入后端（dispatchGesture 单点 tap，约 50ms），Shizuku 不可用时自动回退；
- 高风险编辑器"目标位置"圆角调节；组件最小尺寸 42→38dp。

### v1.3.0（2026-08-17）
- 高风险编辑器虚拟按钮支持圆角形状调节（持久化，运行时一致）；
- 文档化 Shizuku 替代注入方案（本轮仅评估）。

### v1.2.0（2026-08-17）
- 指定应用启动模式下，前台非目标应用时隐藏悬浮开关与屏蔽区（低风险）。

### v1.1.1（2026-08-17）
- 修复触发应用列表行距为 0；前台检测事件窗口 5s→30s→2min→10min 逐级扩大。

### v1.1.0（2026-08-17）
- 权限状态实时反馈（已授权变绿、系统设置返回自动刷新）；勾选"启动指定应用监控"自动拉起服务。

### v1.0.0（2026-08-17）
- 修复状态按钮颜色刷新不及时；新增"指定应用启动"（UsageStatsManager 前台检测 + 触发应用列表），新增 PACKAGE_USAGE_STATS 权限。

### v0.9.0（2026-08-17）
- 悬浮开关透明度拖动即时生效（setToggleAlpha）；拖动时屏蔽区显示半透明红色标识。

### v0.8.0（2026-08-17）
- 首页彩色风险标签；启动/停止合并为单状态按钮；可折叠"高级"模块（方案管理）、透明度调节；高风险二次确认弹窗。

### v0.7.0（2026-08-17）
- 首页入口改名 + 问题反馈区（小红书/bilibili）；首次进入悬浮窗/通知权限引导。

### v0.6.1（2026-08-15）
- 新增操作日志功能（创建/写入/复制/清空）。

### v0.6.0（2026-08-15）
- 新增"权限与运行保障"页面：悬浮窗/通知/电池后台/后台任务锁定四项检查；vivo/iQOO 侧载兼容提示；设备诊断与复制。

### v0.5.1（2026-08-15）
- 修复 Overlay 启动崩溃：圆角屏蔽窗口 32→8 个，创建失败回退单窗口矩形，异常保护。

### v0.5.0（2026-08-14）
- 纯构建门禁版本（lint/assemble/test 全通过）。

### v0.4.0（2026-08-14）
- 引入 Shizuku 集成骨架与 input-test-app 测试应用（高风险链路代码基础）。

### v0.3.0（2026-08-14，首个版本）
- 基础"屏蔽区域"功能：截图编辑器、矩形/圆形屏蔽区、悬浮开关、位置与形状持久化；高风险方案仅占位。

---

## 备忘

- **高风险目标拦截的语义**：目标位置有不可见拦截窗（玩家直点无效）+ 虚拟按钮（向目标矩形几何中心注入一次 Tap）。V6.2 起编辑器目标矩形与运行时屏蔽矩形完全相同，不再隐式放大。
- **已知遗留问题**：滑动经过屏蔽区/目标区会被拦截窗吞掉（v6.0 方案 B 已删除）；无障碍服务在部分设备（OPLUS）会被系统周期性重启。
- **代码基线**：v6.2 = v6.1 代码 - 14%×18% 隐式扩张 + 目标中心单一真源 + 非零不可见输入层 + 长按滑动取消状态机。
