# 游戏按钮映射 V0.6.0 权限与厂商兼容性计划

## 目标

新增统一“权限与运行保障”页面，检测悬浮窗、通知和电池后台状态，提供设置入口、vivo/iQOO 提示、后台锁定说明和运行诊断。

## 边界

- 不改变低风险屏蔽区域、悬浮开关和 Profile 数据结构。
- 不改变高风险单按钮映射、Shizuku 输入链路和测试 App。
- 不自动修改厂商后台策略，不自动锁定最近任务，不绕过系统权限限制。
- 非核心权限存在风险时允许继续启动；悬浮窗权限缺失时禁止启动。

## 阶段门禁

1. 权限检测与设置跳转可在 Android 8.0 及以上编译。
2. 低风险和高风险启动前都经过统一核心权限检查。
3. 服务生命周期和 WindowManager 事件可在 Logcat 与诊断页面中定位。
4. 构建通过；iQOO Neo9 真机验证单独记录，未连接设备时不得宣称通过。

## 当前状态

- [x] 建立权限状态、设置跳转、厂商识别和持久化诊断层。
- [x] 完成权限与运行保障页面及首次启动引导。
- [x] 接入低风险/高风险启动前检查和服务生命周期日志。
- [x] `lintDebug`、`assembleDebug` 和单元测试任务检查完成。
- [ ] iQOO Neo9 真机验证，当前未执行。
