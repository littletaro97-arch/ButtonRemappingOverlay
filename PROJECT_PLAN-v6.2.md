# 游戏按钮映射 V6.2 修复计划

日期：2026-08-18

## 目标

修复修改键位模式的目标区域语义分裂，并修复长按触发模式把滑动误判为长按的问题。V6.2 的唯一权威源码目录为 `E:\按钮重映射`；历史输出和 `codex fix` 仅作对照，不回写。

## 已确认问题

1. V6.1 编辑器显示 `targetBlock`，运行时却通过 `targetInterceptRect()` 把它放大到至少屏幕 14%×18%，显示边界与实际屏蔽边界不一致。
2. V6.1 注入点取放大/夹取后的屏蔽矩形中心；目标靠近屏幕边缘时夹取会移动该中心，因此不能保证点击用户所画目标矩形的几何中心。
3. `LongPressTouchAreaView` 未处理 `ACTION_MOVE`，触点滑离目标区域后计时仍继续，达到阈值就会误触发。
4. V6.1 把目标拦截 View 的 `alpha` 设为 0；虽然透明 View 理论上仍可接收触摸，但部分 OEM 对完全透明 Overlay 的合成/输入处理存在不确定性。V6.2 不依赖完全透明 View alpha，改用几乎不可见但非零的绘制表面。

## 实施规则

### A. 高风险目标几何

- 编辑器显示矩形、保存的 `targetBlock`、运行时 WindowManager 屏蔽矩形必须来自同一次 `OverlayGeometry.toPixelRect()` 结果。
- 删除运行时 14%×18% 隐式扩张；若目标需要覆盖更大的游戏热区，由用户在编辑器中把可见目标矩形调大。
- 模拟点击坐标固定为用户目标矩形的几何中心，不得从其他扩张/夹取矩形派生。
- 目标编辑器按完整矩形表达屏蔽范围；圆角只属于视觉样式，不能暗示角落可穿透。

### B. 长按资格

- 仅 `ACTION_DOWN` 启动一次计时。
- 任一条件出现即永久取消本次手势：移动距离超过系统 `touchSlop`、触点离开区域、出现第二指、系统发送 `ACTION_CANCEL`。
- 取消后即使继续停留超过阈值也不得重新开始或触发；必须抬起并重新按下。
- 合格手势仍维持原有阈值、进度轮廓和单次 Tap 行为。

## 阶段门禁

1. **Gate 0 — 设计一致性**：三套目标几何只有一个源；长按取消状态不可逆直到本次手势结束。
2. **Gate 1 — 静态验证**：版本号 6.2 / versionCode 35；源码搜索不再存在 14%×18% 运行时扩张常量；测试覆盖中心点与滑动取消策略。
3. **Gate 2 — 构建验证**：`testDebugUnitTest lintDebug assembleDebug` 全部通过；APK 元数据和 SHA-256 记录完整。
4. **Gate 3 — 设备验收**：不使用 ADB。真实游戏内的目标直点、虚拟按钮中心点击、边缘目标、滑出长按区域四项必须由用户手工确认；确认前状态为 `NOT RUN`，不得写 PASS。

## 非目标与风险

- 不恢复 V6.0 的滑动补发方案。
- 不承诺 Overlay 暂时让路以发送注入 Tap 时不存在极短竞争窗口；这是当前“屏蔽自身窗口后向下层注入”架构的固有限制，需要设备侧手工压力测试。
- 不修改或删除四个位置中的历史 APK、历史文档和 `codex fix` 快照。
