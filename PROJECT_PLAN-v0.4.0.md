# 游戏按钮映射：V0.4 高风险单按钮映射计划

## 目标

在不接触真实游戏的前提下，验证一条受用户主动控制的单次输入链路：

`虚拟按钮一次人工点击 → Shizuku 系统输入能力 → input-test-app 指定坐标收到一次点击`

本版本只支持一个虚拟按钮、一个目标坐标和 Tap。低风险方案必须保持独立并继续可用。

## 明确禁止项

- 真实游戏安装、启动或输入验证
- 连点、宏、长按、滑动、摇杆、多按钮和自动操作
- OCR、图像识别、AI 状态识别、自动瞄准或自动移动
- Root、Magisk、Accessibility `dispatchGesture`、Hook、修改目标 App/内存/APK
- 隐藏通知、后台偷偷启动、开机自动注入和反作弊规避

## 实现边界

1. `lowrisk` 只负责局部 Overlay 屏蔽，不依赖任何注入类；其现有服务和配置行为不得被高风险模块改写。
2. `highrisk` 单独负责配置、虚拟按钮 Overlay、Shizuku 状态和一次性 Tap 请求。
3. 注入必须通过 Shizuku UserService 运行的系统输入能力；权限、服务断开、配置失效或注入异常时立即停止映射。
4. 虚拟按钮只接受短按；长按、拖动和取消事件不得生成目标点击。
5. input-test-app 是唯一输入接收测试对象，必须记录 `ACTION_DOWN`、`ACTION_UP`、坐标、时间和累计次数。

## 阶段门禁

### V0.4-A：环境与权限门禁

确认官方 Shizuku 组件、设备 Android 版本、服务状态和授权流程；记录未安装、未运行或 API 不可用的具体原因。未通过前不宣称注入可用。

### V0.4-B：配置与 Overlay 门禁

实现高风险入口、警告页、目标区域/虚拟按钮编辑、比例配置、启动/停止状态。此阶段不发送任何输入事件。

### V0.4-C：Shizuku Tap 门禁

实现单次 Down/Up 注入、异常停止和服务生命周期保护；不实现其他手势或自动操作。

### V0.4-D：测试应用与设备验收

只安装主 App、Shizuku 和 `input-test-app`。完成单击、连续点击、触摸隔离、停止后无残留、Crash/ANR/SecurityException/Logcat 检查。真实游戏测试永远不执行。

## 交付判定

- 若 Shizuku 在设备上可用：提供实际 1:1 测试证据。
- 若 Shizuku 不可用：交付可编译的权限/状态/故障保护界面，并将输入链路和设备验收标记为“未验证”，不得用理论推断代替通过。
