# 游戏按钮映射 V0.5.1 Overlay 启动兼容性修复计划

## 问题

部分 Android 设备在启动低风险悬浮层后，开关短暂显示随后消失，屏蔽区也没有生效。当前 `OverlayService` 在添加多个圆角屏蔽窗口时存在异常未覆盖路径，服务可能因此退出。

## 修复范围

- 保持低风险功能边界：一个屏蔽区域、一个开关，不加入输入注入或自动操作。
- 降低启动时创建的 Overlay 窗口数量，避免 OEM 对多窗口 Overlay 的限制。
- 覆盖屏蔽区创建失败、WindowManager 状态变化和服务启动异常。
- 记录明确的 Logcat 原因，便于用户在其他设备反馈实际异常。

## 阶段门禁

1. 代码门禁：启动链路和屏蔽区创建不再把可恢复的 WindowManager 异常抛出到服务外部。
2. 构建门禁：`lintDebug` 与两个 Debug APK 构建通过。
3. 设备门禁：本轮不代替用户进行其他手机验证；未执行的设备结果必须明确标记。

## 当前状态

- [x] 完成启动链路审计。
- [x] 完成多窗口降级和服务异常保护。
- [x] 完成 Logcat 故障信息补充。
- [x] `lintDebug` 与 `assembleDebug` 通过。
- [ ] 其他 Android 手机实测，未执行。
