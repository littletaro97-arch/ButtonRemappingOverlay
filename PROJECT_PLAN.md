# 游戏按钮映射：V0.3 截图辅助低风险方案计划

## 当前目标

在 V0.2 单区域低风险 Overlay 基础上，增加“上传本机游戏截图 → 将截图原样铺满编辑区 → 在截图上编辑一个屏蔽区域 → 运行时恢复同一全屏位置”的流程。应用名称正式为“游戏按钮映射”。

当前版本仍然只屏蔽一个触摸区域，不发送任何触摸事件，不实现按钮映射或自动操作。

## 明确禁止项

- 模拟点击、输入注入、InputManager、MotionEvent 注入
- Root、Shizuku、Magisk、Accessibility 自动点击
- ADB 输入控制、自动点击、连点、宏、连招
- OCR、AI 识别、实时游戏画面分析、自动寻找按钮
- 修改游戏数据、规避反作弊

## 技术决策

1. 继续使用 Kotlin + 原生 Android View，复用 V0.2 的 `ComponentEditorView`、Overlay 服务、比例坐标和悬浮开关。
2. 使用系统 `ACTION_OPEN_DOCUMENT` 选择 PNG/JPG 等图片，并持久化 URI 读取权限。
3. 读取图片原始宽高，截图编辑器将截图原样铺到当前设备全屏画布；内部仍保存归一化坐标，便于持久化，不做额外比例适配流程。
4. 保存截图 URI、原始宽度、原始高度和屏蔽矩形比例；Overlay 运行时不需要解码图片。
5. 本机截图与当前设备全屏尺寸一致时，运行时直接按同一全屏画布恢复区域；尺寸不一致时只提示重新截图，不做留白或二次缩放。
6. 运行态继续只创建局部透明屏蔽窗口和小型悬浮开关；开关启动、拖动和保存时均自动避开屏蔽区，并保持安全区限制。
7. 屏蔽区域支持圆形/圆角到正方形的形状调节；运行态只拦截形状内部，不扩大为整块矩形。
8. 高风险方案只保留首页入口和 `HighRiskManager` 占位，不加入任何输入映射逻辑。

## 阶段门禁

### V0.3-A：截图选择与截图编辑

范围：应用名称更新、图片选择、图片尺寸读取、截图原样全屏预览、单矩形拖动/缩放、归一化坐标保存。

门禁：只支持一个矩形；编辑器不能创建运行态 Overlay；不加入 OCR、AI 或自动识别；截图无法读取时不得保存伪造尺寸。

### V0.3-B：全屏坐标恢复与运行态

范围：截图同尺寸全屏坐标恢复、尺寸提示、局部透明拦截、形状拦截、悬浮开关复用与碰撞避让。

门禁：截图应来自当前设备；不创建全屏吞触摸窗口；屏蔽区域外触摸保持可用；开关不得与屏蔽区重叠；屏蔽 OFF 时移除全部屏蔽窗口；不调用任何输入注入 API。

### V0.3-C：设备回归

范围：ADB 安装、截图选择、截图编辑保存/恢复、尺寸不一致提示观察、Overlay 开关。

门禁：本地构建、静态检查和设备观察分开记录；未实际操作的设备流程不得标记为通过。

## 当前不做

多按钮、多截图配置、配置分享、AI/OCR、自动适配游戏界面、输入映射、宏、连点、长按映射和任何自动操作均不在 V0.3 范围内。
