# 游戏按钮映射 v6.6 修复计划

## 目标

1. 修复编辑器与运行时在相反横屏方向间出现 180 度坐标翻转。
2. 提供可切换的“从最近任务隐藏”功能，但不把它描述为后台保活能力。
3. 在“运行必须”中增加全屏显示检查和设置入口。
4. 修复屏蔽区编辑器没有进入完整沉浸式全屏的问题，并记录华为 EMUI 的系统限制。

## 范围与约束

- 正式基线：v6.5（versionCode 38）。
- 本轮不修改 `input-test-app` 和 `touch-interaction-probe` 的既有未提交实验代码。
- 只使用 Android 公开 API；不硬编码未经官方文档保证的华为 Settings Activity 类名。
- “从最近任务隐藏”只能降低用户从最近任务手动划掉应用的概率，不能阻止系统杀进程。
- 华为“应用全屏显示”属于厂商设置。公开 API 无法查询其开关或直达具体应用项；按用户接受的方案，检测实际窗口覆盖并打开通用显示设置。

## 阶段门禁

1. **Gate 1 — 坐标语义**：相反横屏方向使用相同逻辑坐标；横竖屏互转保持可逆；截图编辑器不得在保存时覆盖已经换算好的坐标方向。
2. **Gate 2 — 编辑器全屏**：全部区域编辑器同时隐藏状态栏与导航栏，并允许边缘滑动临时唤出。
3. **Gate 3 — 设置能力**：检测实际窗口覆盖状态；异常时打开系统“显示”设置。仅明确识别为华为的设备显示华为操作路径；荣耀及其他厂商只显示通用说明。用户已明确接受该公开 API 方案。
4. **Gate 4 — 最近任务**：开关可即时更新当前任务的最近任务可见性，并可恢复。
5. **Gate 5 — 构建许可**：Gate 1–4 全部通过后，才允许把版本号改为 v6.6 并运行 Gradle 构建。

## 验收边界

- 没有目标华为 nova 5 Pro 时，设备侧全屏表现只能标记为 `NOT RUN`。
- 不硬编码厂商私有 Settings Activity；设置入口保持公开 API 和安全回退。
