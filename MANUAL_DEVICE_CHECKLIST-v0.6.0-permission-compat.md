# 游戏按钮映射 V0.6.0 真机验收清单

## 权限页面

1. 打开首页，进入“权限与运行保障”。
2. 确认悬浮窗、通知、电池和后台任务锁定四项显示正确。
3. 从每个系统设置页返回，确认状态自动刷新。
4. 在 Android 13 及以上点击“允许通知”，确认权限状态变化。
5. 点击“复制诊断信息”，确认包含设备、Android、系统、权限和 Service 状态。

## iQOO Neo9 / vivo

1. 记录实际 Android 版本和 OriginOS 版本。
2. 如果系统出现侧载应用权限受限提示，手动解除限制。
3. 确认悬浮窗权限、电池后台运行和通知权限。
4. 在最近任务中锁定“游戏按钮映射”（如果系统提供锁定功能）。
5. 启动低风险屏蔽，等待至少 30 秒。
6. 切换前后台、锁屏、解锁，确认开关和屏蔽区仍存在。
7. 如仍消失，复制诊断信息并抓取 `ButtonOverlay`、`RuntimeProtection`、`AndroidRuntime`、`WindowManager` 日志。

## 高风险

仅使用自建 input-test-app 验证。不得使用真实游戏。
