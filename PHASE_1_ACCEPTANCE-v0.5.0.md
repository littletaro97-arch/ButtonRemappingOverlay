# 游戏按钮映射 V0.5 Phase 1 验收记录

## 已实现

- 低风险和高风险共用 `ProfileManager`。
- 每种模式保留一个默认方案，并按模式分别维护当前方案。
- 新建方案支持空白方案或复制当前方案；创建后进入对应编辑器。
- 下拉框切换当前方案；运行中的低风险 Overlay / 高风险映射会尝试按新方案重启。
- 方案列表支持选择管理对象；当前使用方案禁止删除。
- 低风险保存一个屏蔽区域和一个悬浮开关；高风险保存一个目标位置和一个虚拟按钮。
- 旧 `layout_config` / `single_mapping_config` 仍保留，并在首次初始化对应模式时迁移到默认方案。

## 未执行

- 未安装 APK。
- 未在设备上验证新建、复制、切换、删除保护或旧数据迁移。
- 未启动真实游戏，也未进行任何真实游戏输入验证。
